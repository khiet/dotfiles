gui:
  showIcons: true
  scrollHeight: 8
  scrollPastBottom: false
  showRandomTip: false
  showBottomLine: true
  splitDiff: 'always'
git:
  branchLogCmd: "git lg"
  paging:
    colorArg: always
    pager: delta --diff-so-fancy --dark --paging=never
notARepository: 'skip'
