---
description: Boot the app and load every route or screen this branch changed, one screenshot each, reporting anything that fails to render
---

# Smoke Test

Use the `smoke-test` skill. Routes or screens given as arguments pass through to it.

Usage: `/smoke_test [route-or-screen ...]`

$ARGUMENTS
