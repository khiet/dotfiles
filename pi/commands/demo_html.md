---
description: Build a shareable HTML demo of a feature, matched to the live product, and publish it as an Artifact for the team to agree on the UI/UX
argument-hint: "<feature description | issue-ref | artifact-url> [instructions]"
---

# Demo HTML

Use the `demo-html` skill. The argument passes through to it.

Usage: `/demo_html <feature description | issue-ref | artifact-url> [instructions]`

$ARGUMENTS
